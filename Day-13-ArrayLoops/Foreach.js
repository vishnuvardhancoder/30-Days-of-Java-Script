let num =[23,44,56,34,12];
num.forEach((n,i,num)=>{ //foreach fetch one element at a time from the array, and we can perform operations.
    console.log(n,i,num);
})
//we can pass three parameters to foreach method a variable,index,and the array itself .