let data = [1,2,3];
console.log(data.push(4)); //push method adds elements to the last of the array.
console.log(data.pop()); //pop mehtod removes the last element of the array.
console.log(data.shift()); //shift method removes the first element of the array the array is shiffted one block left
console.log(data.unshift(0)); //unshift method adds elements to the first of the array
console.log(data.splice(1,1, 5)); //splice used to remove and add value from inbetween the array we should pass the index and count to be removed.
console.log(data);