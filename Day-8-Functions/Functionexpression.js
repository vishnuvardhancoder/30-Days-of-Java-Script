//expression - a + b it evaluates and assigns the result to a variable

let add = function(num1,num2){ //this is anonymous function which has no name but assigend to variable add now we call add to access this function
    return num1 + num2;
}
console.log(add(5,2)) 