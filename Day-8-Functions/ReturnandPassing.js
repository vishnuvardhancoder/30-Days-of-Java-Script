function Greet(user){
    return `Hello ${user}`  //return statement returns a value there should be one return statement for a function after execution of return statement control will come out of function
}
let user = "Programmer"
let str = Greet(user)
console.log(str);